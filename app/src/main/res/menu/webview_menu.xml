<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item android:id="@+id/action_search"
        android:title="Search"
        android:icon="@drawable/dr_icon_search"
        app:showAsAction="ifRoom" />

    <item android:id="@+id/action_next"
        android:title="Next match"
        android:icon="@drawable/next_button"
        app:showAsAction="never" />

    <item android:id="@+id/action_previous"
        android:title="Previous match"
        android:icon="@drawable/previous_button"
        app:showAsAction="never" />

    <item android:id="@+id/action_clear_search"
        android:title="Clear search"
        android:icon="@drawable/icon_clear"
        app:showAsAction="never" />
</menu>